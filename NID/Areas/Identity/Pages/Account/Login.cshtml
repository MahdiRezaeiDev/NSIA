@page
@model LoginModel

@{
    ViewData["Title"] = "ورود به حساب کاربری";
}
<div class="d-flex justify-content-center align-items-center h-100">
    <div class="card rounded-3 text-black h-100">
        <div class="row g-0">
             <div class="col-lg-6">
                                <div class="card-body p-md-5 mx-md-4">

                                    <div class="text-center">
                                        <img src="~/img/logo.png" asp-append-version="true"
                                             style="width: 185px;" alt="logo">
                                        <h4 class="mt-1 mb-5 pb-1">اداره ملی احصائیه و معلومات</h4>
                                    </div>

                                    <form id="account" method="post">
                                        @* <p>لطفا وارد حساب کاربری خود شوید.</p> *@
                                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                                        <div data-mdb-input-init class="form-outline mb-4">
                                            <label class="form-label" asp-for="Input.Email">ایمیل آدرس</label>
                                            <input asp-for="Input.Email" type="email" class="form-control"
                                                   placeholder="ایمیل آدرس خود را وارد نمایید." />
                                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                                        </div>

                                        <div data-mdb-input-init class="form-outline mb-4">
                                            <label asp-for="Input.Password" class="form-label">رمز عبور</label>
                                            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="رمز عبور خود را وارد نمایید." />
                                            <span asp-validation-for="Input.Password" class="text-danger"></span>
                                        </div>

                                        <div class="text-center pt-1 mb-5 pb-1">
                                            <button data-mdb-button-init data-mdb-ripple-init class="btn btn-primary btn-block fa-lg gradient-custom-2 mb-3" type="submit">
                                               ورود به حساب
                                            </button>
                                            @* <a class="text-muted" href="#!">رمز عبور خود را فراموش کرده اید؟</a> *@
                                        </div>
                                    </form>

                                </div>
                            </div>
                            <div class="col-lg-6 d-flex align-items-center gradient-custom-2">
                                <div class="text-white px-3 py-4 p-md-5 mx-md-4">
                                    <h4 class="mb-4">اداره ملی احصائیه و معلومات</h4>
                                    <p class="small mb-0">
                        سیستم تذکره آنلاین اداره ملی احصاییه و معلومات، بستری امن و سریع برای مدیریت و ارائه اطلاعات هویتی شهروندان افغانستان فراهم می‌کند. 
                        هدف ما سهولت دسترسی به اطلاعات، کاهش کاغذبازی، و ارتقای شفافیت در خدمات دولتی است.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
